# DESC In Kind Portal App

### Description

A prototype frontend to integrate with the DESC Simplify API

## Install
To install the required dependencies, run:

```
npm install
```

## Run
After installing the dependencies, run:
```
npm run dev
```